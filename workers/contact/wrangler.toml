name = "lsg-contact"
main = "index.js"
compatibility_date = "2024-01-01"
workers_dev = true

# Custom domain route (configure in Cloudflare dashboard)
# routes = [{ pattern = "contact.littlesmartgenius.com/*", zone_name = "littlesmartgenius.com" }]

# Secrets (set via: npx wrangler secret put SECRET_NAME)
# MAILERLITE_API_KEY
# MAILERLITE_GROUP_ID
# ADMIN_EMAIL
# SITE_URL
